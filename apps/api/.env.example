# Copy this file to .env and fill in the values.
# Never commit the real .env to version control.

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=customer_registration

# Prisma – automatically composed by docker-compose.
# For local dev without Docker, set this manually:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/customer_registration
